# Sandbox Playground

This directory contains a lightweight playground entry for freeform UI testing.

## Run

From the workspace root:

```bash
pnpm run dev:sandbox
```

This opens `sandbox/index.html`, which boots `src/sandbox/main.ts` and renders an interactive canvas with text, buttons, and form inputs.

