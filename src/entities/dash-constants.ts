/** How long, in milliseconds, an active dash lasts. */
export const DASH_DURATION_MS = 250;

/** Factor {@link MovementController}'s base movement speed is scaled by during an active dash. */
export const DASH_SPEED_MULTIPLIER = 4.2;

/** How long, in milliseconds, after a dash ends before another can start. */
export const DASH_COOLDOWN_MS = 450;
