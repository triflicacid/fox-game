# Display Sandbox

This directory contains a lightweight playground entry for freeform display-lib UI testing.

## Run

From `lib/display`:

```bash
pnpm run dev:sandbox
```

This opens `sandbox/index.html`, which boots `sandbox/main.ts` and renders an interactive canvas with text, buttons, and form inputs.

