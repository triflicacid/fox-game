{
  "name": "fox-game",
  "version": "1.0.0",
  "description": "a small typescript fox game",
  "main": "dist/electron/main.js",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "clean": "rm -r -fo dist",
    "dev:client": "vite",
    "copy:static": "cp static dist/web -r -Force",
    "build:client": "tsc -p tsconfig.json && vite build && pnpm run copy:static",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "electron": "tsc -p electron/tsconfig.json && electron dist/electron/main.js",
    "electron:rebuild": "electron-rebuild -f -w ."
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.1",
    "eslint": "^10.7.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.64.0",
    "vitest": "^3.2.4",
    "vite": "^8.1.4",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "private": true,
  "dependencies": {
    "electron": "^43.1.1"
  }
}
